const arr = ['alvi', 'adam', 'djabrail', 'islam', 'ayub', 'movsar', 'isa', 'ibragim', 'muhammad', 'salambek'];
for (let i = 0; i < arr.length; i++) {
    if (arr[i][0] === 'a') {
        console.log(arr[i]);
    }
};

