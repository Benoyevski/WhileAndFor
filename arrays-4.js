let arr = ['javaScript', 'python', 'c++', 'c#', 'basic', 'pascal', 'java', 'php', 'swift', 'go'];
let i = 0;
while (i < arr.length) {
    if (arr[i].length > 3) {
        console.log(arr[i]);
        i++;
    } else {
        i++;
    }
}
